import * as d3Format from 'd3-format';

import {
  format,
  formatPrefix,
  FormatSpecifier,
  formatDefaultLocale,
  formatLocale,
  formatSpecifier,
  precisionFixed,
  precisionPrefix,
  precisionRound,
} from 'd3-format';

export {
  d3Format,
  /* */
  format,
  formatPrefix,
  FormatSpecifier,
  formatDefaultLocale,
  formatLocale,
  formatSpecifier,
  precisionFixed,
  precisionPrefix,
  precisionRound,
};

export default d3Format;
