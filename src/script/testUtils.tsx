import React from 'react';

import 'jest-enzyme';

import { RecoilRoot } from 'recoil';
import { create } from 'react-test-renderer';
import { mount } from 'enzyme';

export { RecoilRoot, create, mount };

export default React;
